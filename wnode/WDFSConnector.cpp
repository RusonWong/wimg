#include <iostream>
#include <fstream>
#include "LocalStorage.h"
using namespace std;


int WDFSConnector::save_file(const char* buff, size_t file_size, std::string file_name)
{
	
	return 1;
}
int WDFSConnector::get_file(std::string key, char* &buffptr,size_t& len)
{
	return 1;
}